version: '3'
services:
  app:
    build: ./app
    ports:
      - "8008:8000"
    env_file:
      - .env.local
    volumes:
      - ./app:/app
    depends_on:
      - redis
    networks:
      - behandelr_network
    command: ["bash", "/app/deploy/docker-entrypoint.development.sh"]

  redis:
    image: redis
    networks:
      - behandelr_network
    ports:
      - "6382:6379"

networks:
  behandelr_network:
    external: true
